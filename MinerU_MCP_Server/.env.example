# MinerU Service URL
# Example: http://localhost:8080/file_parse
# Note: The endpoint is /file_parse, not /parse
MINERU_URL=http://your-mineru-service:8080/file_parse

# Transport mode (HTTP only)
# The server now only supports HTTP transport. Leave as http.
TRANSPORT=http

# Optional: Temporary directory for downloaded files
# TMPDIR=/tmp
